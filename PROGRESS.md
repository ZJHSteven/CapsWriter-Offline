# 项目状态快照

## 当前结论（必须最新）
- 现状：已在 GitHub 创建用户 fork（`ZJHSteven/CapsWriter-Offline`），并建立本地工作副本；当前已同步本地快照内容。
- 已完成：
  - 核对阿里云百炼官方文档（实时 WebSocket、录音文件 REST）。
  - 核对 Context7 可用源：`/websites/help_aliyun_zh_model-studio`、`/modelscope/funasr`。
  - 建立执行计划文档 `PLANS.md`。
- 正在做：将当前本地快照作为基线提交到 fork，并准备创建改造分支。
- 下一步：
  - 完成基线 commit/push。
  - 开始接入百炼实时 WebSocket ASR。
  - 设计文件转写 REST 异步通道，保证不阻塞实时听写。

## 关键决策与理由（防止“吃书”）
- 决策A：实时听写主链路采用 WebSocket，而不是仅 REST。
  - 原因：当前项目是输入法实时交互；REST 文件转写接口要求 URL 输入且是异步任务，不适合作为麦克风主链路。
- 决策B：文件转写走 REST 异步，作为独立通道。
  - 原因：可利用官方文件识别能力与可选时间戳能力，同时避免拖慢实时链路。
- 决策C：优先保持客户端协议不变，只在服务端做适配。
  - 原因：最大限度复用已有热词、后处理、上屏、LLM 等成熟能力，减少回归风险。

## 常见坑 / 复现方法
- 坑1：REST 文件识别不支持本地文件直传与 base64。
  - 复现：直接把本地路径或 base64 放到 `input`，接口会失败。
- 坑2：文件转写与实时听写共用同一阻塞执行路径会互相影响。
  - 复现：启动长视频转写后，按热键录音会出现响应变慢或等待。
